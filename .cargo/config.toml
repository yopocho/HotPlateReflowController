[target.thumbv6m-none-eabi]
runner = 'probe-rs run --chip STM32C071FBPx'

[build]
target = "thumbv6m-none-eabi"

[env]
DEFMT_LOG = "info"

[unstable]
build-std = ["core"]
build-std-features = ["panic_immediate_abort"]
